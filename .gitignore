# Terraform state — never commit
*.tfstate
*.tfstate.backup
*.tfstate.lock.info
.terraform/
.terraform.lock.hcl
tfplan

provision-out

# Secrets — never commit
terraform.tfvars
*.auto.tfvars

# macOS
.DS_Store

dist/
node_modules/
